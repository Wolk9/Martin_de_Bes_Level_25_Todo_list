// DOM manipulation
const grabSection = document.querySelector("section"); // grab Place where the UI will appear

const createInputDiv = document.createElement("div"); // create the div for the inputfield

const createTableDiv = document.createElement("div"); // create the div for the table with data

const createTable = document.createElement("table"); // create the table itself
